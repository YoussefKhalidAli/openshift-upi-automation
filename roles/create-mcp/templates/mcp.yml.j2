apiVersion: machineconfiguration.openshift.io/v1
kind: MachineConfigPool
metadata:
  name: {{ item.name }}
spec:
  machineConfigSelector:
    matchExpressions:
      - key: machineconfiguration.openshift.io/role
        operator: In
        values:
          - worker
          - {{ item.role }}
  nodeSelector:
    matchLabels:
      node-role.kubernetes.io/{{ item.label }}: ""